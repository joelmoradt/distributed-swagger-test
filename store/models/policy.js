/**
 * @swagger
 * components:
 *   schemas:
 *      Policy:
 *        type: object
 *        properties:
 *          id:
 *            type: string
 *          number:
 *            type: string
 *          planCode:
 *            type: string
 *          productOptions:
 *            $ref: '#/components/schemas/PolicyOptions'
 */
class Policy {
  // id
  // number
  // planCode
  // productOptions
}
